"use strict";

/*function enableLog() {
	var coroutine = require('coroutine');
	coroutine.current().logEnabled = true;
}
exports.enableLog = enableLog;

function disableLog() {
	var coroutine = require('coroutine');
	coroutine.current().logEnabled = false;
}
exports.disableLog = disableLog;

//获取当前线程的log开关
function logEnabled(){
	var coroutine = require('coroutine');
	var logEnabled = coroutine.current().logEnabled
	return logEnabled === undefined || logEnabled;
}
exports.logEnabled = logEnabled;*/
